{
  "name": "ram-lead-router",
  "version": "0.1.0",
  "private": true,
  "description": "RAM Lead Router — turns completed Vapi AI receptionist calls into delivered business leads.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "dev": "node scripts/local-server.js",
    "send-fixture": "node scripts/send-fixture.js"
  }
}
