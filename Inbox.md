# Inbox

Dump anything here — task, idea, expense, note. Don't sort, don't decide,
just capture. Under 10 seconds. Clear this to empty during daily Clarify
(turn each line into a task in `Tasks.md`, a note in a `Pillars/` file, or
delete it).

-
